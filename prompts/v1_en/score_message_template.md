Requirement Scoring Results:

{score_groups}

Score Legend: 2=Fully compliant, keep unchanged; 1=Generally compliant, keep or polish; 0=Partial coverage, rewrite; -1/-2=Deviation/conflict, redesign and rewrite
Action Required: Score<=0 must rewrite with same ID; Score=1 can keep or optimize with same ID; Score=2 keep unchanged and do not output again

Task Requirements:
1. For requirements with Score <= 0 (partial coverage, deviation, or conflict), you must regenerate improved versions using the same ID
2. For requirements with Score = 1 (generally compliant), you can keep them unchanged or make minor optimizations using the same ID
3. For requirements with Score = 2 (fully compliant), keep them unchanged and do not output them again
{new_requirements_instruction}
