# 需求挖掘问题分析报告

## 问题描述
系统只挖掘了7个需求（REQ-000到REQ-006），后续迭代中虽然AI生成了新需求（如REQ-007到REQ-012），但系统显示"新增需求数量: 0"，导致最终只有7个需求。

## 根本原因

### 1. 格式不一致问题

**第一次迭代（成功）：**
- AI返回格式：`REQ-000:`（纯文本，无Markdown标记）
- 解析代码能正确识别

**后续迭代（失败）：**
- AI返回格式：`**REQ-007: 离线数据同步机制**`（带Markdown粗体标记`**`）
- 解析代码无法识别

### 2. 解析逻辑缺陷

在 `src/agents/req_explore.py` 第214-220行：

```python
# 检查是否是新的需求ID行 "REQ-XXX:"
if ":" in line:
    parts = line.split(":", 1)
    req_id = parts[0].strip()  # 对于 "**REQ-007: ..." 会得到 "**REQ-007"
    
    # 验证ID格式
    if req_id.startswith("REQ-"):  # "**REQ-007" 不满足此条件！
```

**问题：**
- 当AI返回 `**REQ-007: 离线数据同步机制**` 时
- `req_id = parts[0].strip()` 得到 `**REQ-007`
- `req_id.startswith("REQ-")` 检查失败（因为实际是 `**REQ-`）
- 因此该需求被当作普通文本处理，没有被解析为需求

### 3. 日志证据

从日志文件 `output/cctns-013/srs_gen_20251115_150613.log` 可以看到：

**第一次迭代（第176行）：**
```
REQ-000:
**功能描述**：系统必须支持离线模式运行...
```
✅ 格式正确，成功解析

**第二次迭代（第811行）：**
```
**REQ-007: 离线数据同步机制**
- **功能描述**: 确保系统在离线模式下数据采集的完整性和后续同步
```
❌ 格式带Markdown标记，解析失败

**系统日志（第911行）：**
```
2025-11-15 15:08:06 [INFO] [Main] 挖掘完成，新增需求数量: 0
```
确认没有识别到新需求

## 解决方案

### 方案1：增强解析逻辑（推荐）

修改 `src/agents/req_explore.py` 的解析代码，在提取需求ID时去除Markdown格式标记：

```python
# 检查是否是新的需求ID行 "REQ-XXX:"
if ":" in line:
    parts = line.split(":", 1)
    req_id = parts[0].strip()
    
    # 去除Markdown格式标记（**、*、`等）
    req_id = req_id.replace("**", "").replace("*", "").replace("`", "").strip()
    
    # 验证ID格式
    if req_id.startswith("REQ-"):
        # ... 后续处理
```

### 方案2：改进Prompt提示

在 `req_explore.py` 的Prompt中明确要求AI不要使用Markdown格式标记需求ID：

```python
6. 输出格式：每个需求以 "REQ-XXX:" 开头（不要使用Markdown粗体标记），后跟详细描述...
```

### 方案3：双重保障

同时采用方案1和方案2，既改进解析逻辑，又优化Prompt提示，确保兼容性。

## 影响范围

- **受影响的功能**：需求挖掘（ReqExploreAgent）
- **影响程度**：严重 - 导致后续迭代无法添加新需求
- **修复优先级**：高

## 建议修复步骤

1. 修改 `src/agents/req_explore.py` 第217行，添加Markdown标记清理逻辑
2. 更新Prompt提示，明确格式要求
3. 添加单元测试，覆盖带Markdown标记的需求解析场景
4. 重新运行测试用例，验证修复效果

